Command to compile the entire project
cd to src directory
then run: `javac Main.java models/*.java services/*.java utils/*.java`

after compilation, to run the program: java Main
The program will run `java Main`
